spring:
  sql:
    init:
      mode: never

firebase:
  service-account: ${FIREBASE_SERVICE_ACCOUNT}

logging:
  level:
    root: WARN
    org.springframework.web: WARN
    com.volleyball.finder: WARN
    org.springframework.security: ERROR
    org.springframework.security.oauth2: ERROR
    com.nimbusds: ERROR